.DS_Store
.env
.env.*
*.token
credentials
credentials.*
*_secret*
*_secrets*

# Raw upstream source exports — never publish. Customer review text, Play
# Console deep-links, internal MoM spreadsheet data. Only generated reports
# under lisa/ and reports/ are public.
data/input/

# Internal-only briefs (budget, headcount, hiring strategy) — never commit,
# never publish. Local reference only.
internal/

# Python bytecode
__pycache__/
*.pyc
